using Ninject;

namespace TemplateFramework.Pipeline.Contexts
{
    public interface IExecutionContext
    {
        public interface IExecutionContext
        {
            void AddContextBindings(IKernel kernel);

            void RemoveContextBindings(IKernel kernel);
        }
    }
}
